#include <stdio.h>

void main()
{ 	const char *c="Anubhav is trainer\n";
	printf("%s\n",c);
	const char *p;

	//for (int i=0; i<strlen(*c);i++)
		while(c=' ')
		{ strcpy(p,c);
		printf("%s\n",p);
		}
 	 

}

